############################################################
# CORE DATA / NUMPY STACK
############################################################
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.13,<2

############################################################
# MACHINE LEARNING & MODELING
############################################################
scikit-learn>=1.5,<2

############################################################
# RECOMMENDER SYSTEM LIBRARIES
# Pick and choose; you don't need all of them for every project.
############################################################
#implicit>=0.7,<1           # Matrix factorization (ALS/BPR) on implicit data
#lightfm>=1.17,<2           # Hybrid recsys (content + collaborative)
#scikit-surprise>=1.1.4,<2  # Classical CF algorithms; great for experimentation
#lenskit>=0.14,<1          # Academic/experimental toolkit (optional; comment-in if used)

############################################################
# DATA ACCESS / FORMATS
############################################################
# pyarrow>=17,<18            # Arrow/Parquet I/O (often needed for large Kaggle sets)
# fastparquet>=2024.5.0      # Alt parquet engine (optional; pandas will pick available)
# openpyxl>=3.1,<4           # Read Excel metadata files if Kaggle data uses them

############################################################
# NOTEBOOK / INTERACTIVE WORK
############################################################
ipykernel>=6.29,<7
# jupyterlab>=4.2,<5
# notebook>=7.2,<8           # Classic interface; still useful
# ipywidgets>=8.1,<9         # Interactive widgets for exploration
# tqdm>=4.66,<5              # Progress bars for training loops

############################################################
# VISUALIZATION (optional but handy during EDA)
############################################################
matplotlib>=3.9,<4
seaborn>=0.13,<1           # Higher-level plotting; optional

############################################################
# UTILITIES
############################################################
# python-dotenv>=1.0,<2      # Load secrets/paths from .env
# click>=8.1,<9              # Simple CLI commands
# pydantic>=2.8,<3           # Config/data validation

############################################################
# PERFORMANCE / OPTIONAL EXTRAS
############################################################
#polars>=1.5,<2            # Lightning-fast DataFrame (comment-in if desired)
#numba>=0.60,<1            # JIT acceleration (some custom algorithms)

############################################################
# TESTING / LINT / DEV TOOLS (comment out for production)
############################################################
# pytest>=8.3,<9
# pytest-cov>=5.0,<6
# black>=24.0.0              # auto-format
# isort>=5.13,<6
# mypy>=1.10,<2

############################################################
# PIN YOUR PROJECT ITSELF (editable install)
############################################################
# -e .
